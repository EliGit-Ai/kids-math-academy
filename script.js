(()=>{'use strict';document.getElementById('year').textContent=new Date().getFullYear();const reduce=matchMedia('(prefers-reduced-motion:reduce)').matches;const reveals=document.querySelectorAll('.reveal');if('IntersectionObserver'in window&&!reduce){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.1,rootMargin:'0px 0px -40px'});reveals.forEach(el=>io.observe(el))}else reveals.forEach(el=>el.classList.add('visible'));const filters=document.querySelectorAll('.filterbar button'),posters=document.querySelectorAll('.poster');filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;posters.forEach((p,i)=>{const show=f==='all'||p.dataset.cat===f;p.hidden=!show;if(show&&!reduce)p.animate([{opacity:0,transform:'translateY(18px) scale(.98)'},{opacity:1,transform:'none'}],{duration:420,delay:i*45,easing:'cubic-bezier(.2,.8,.2,1)'})})}));if(matchMedia('(pointer:fine)').matches&&!reduce){posters.forEach(card=>{card.addEventListener('pointermove',e=>{const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateY(${x*-3}deg) rotateX(${y*3}deg) translateY(-7px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});const show=document.querySelector('.hero-showcase');show?.addEventListener('pointermove',e=>{const r=show.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;show.querySelectorAll('.orb,.glass-math').forEach((el,i)=>el.style.translate=`${x*(i+1)*5}px ${y*(i+1)*5}px`)});show?.addEventListener('pointerleave',()=>show.querySelectorAll('.orb,.glass-math').forEach(el=>el.style.translate=''))}const form=document.getElementById('updateForm'),message=document.getElementById('formMessage');form?.addEventListener('submit',e=>{e.preventDefault();const email=form.elements.email.value.trim(),grade=form.elements.grade.value;if(!email||!grade)return;const rows=JSON.parse(localStorage.getItem('pedagogi-update-interest')||'[]');rows.push({email,grade,createdAt:new Date().toISOString()});localStorage.setItem('pedagogi-update-interest',JSON.stringify(rows));message.textContent='נרשמתם ✓  נעדכן כשיהיה משהו חדש לשחק.';const b=form.querySelector('button');b.textContent='אתם בפנים ✓';b.disabled=true})})();